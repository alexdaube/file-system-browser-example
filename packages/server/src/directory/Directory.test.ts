// When starting...
// construct directory

// Directory.init(arguments, default_root_directory);

// 1. Procedure to extract root directory
// Remove first 2 arguments from argv
// const arguments = process.argv.slice(2, process.argv.length);

// 2. Get all the directory friendly arguments after that.
// export const extractDirectoryArguments = (): string[] => {
//     return process.argv
//         .slice(2, process.argv.length)
//         .filter((argument: string) => isDirectory(argument));
// };

// 3. Build default directory name

describe('', () => {
  //
});
